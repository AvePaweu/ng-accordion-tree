import { IsIndeterminateDirective } from './is-indeterminate.directive';

describe('IsIndeterminateDirective', () => {
  it('should create an instance', () => {
    const directive = new IsIndeterminateDirective();
    expect(directive).toBeTruthy();
  });
});
